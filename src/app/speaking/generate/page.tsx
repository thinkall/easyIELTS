import { GenerateSpeaking } from "@/components/speaking/GenerateSpeaking";

export const metadata = { title: "Generate a speaking topic — easyIELTS" };

export default function GenerateSpeakingPage() {
  return <GenerateSpeaking />;
}
