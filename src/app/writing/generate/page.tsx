import { GenerateWriting } from "@/components/writing/GenerateWriting";

export const metadata = { title: "Generate a writing test — easyIELTS" };

export default function GenerateWritingPage() {
  return <GenerateWriting />;
}
