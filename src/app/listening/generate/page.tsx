import { GenerateListening } from "@/components/listening/GenerateListening";

export const metadata = { title: "Generate a listening test — easyIELTS" };

export default function GenerateListeningPage() {
  return <GenerateListening />;
}
